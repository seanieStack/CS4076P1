# Aberrations  
## Actions clients can request
`ac` -> add class  
`rc` -> remove class  
`ds` -> display schedule  
`st` -> stop (close connection)  

## Server responces
`ttf` -> timetable full  
`cs`-> class added  
